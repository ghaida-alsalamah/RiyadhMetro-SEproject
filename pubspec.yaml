name: swproj1
description: A new Flutter project.

publish_to: "none" # Remove this line if you intend to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0 # Ensure compatibility with flutter_test
  # Add your other dependencies here

dev_dependencies:
  coverage: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
